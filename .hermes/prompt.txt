You are implementing 4 features in an Android Kotlin app at /home/ayana/anaya.

Tech stack: Kotlin, Jetpack Compose + Material 3, Hilt DI, Room DB, Navigation Compose, GGUF local model.

Key rules:
- Use Hilt for all DI (@Inject constructor + @Singleton)
- 4-space indentation
- DO NOT break existing functionality
- Use Compose Material 3 throughout
- Preferences should use SharedPreferences (simple, no DataStore needed)
- All text in Chinese
- After all changes, verify with ./gradlew assembleDebug

Read the current codebase first, then implement ALL of these:

## Feature 1: Delete All Transactions
1. TransactionDao: add `@Query("DELETE FROM transactions") suspend fun deleteAll()` 
2. TransactionRepository interface: add `suspend fun deleteAll()`
3. TransactionRepositoryImpl: implement deleteAll() that:
   - Gets all transactions
   - Reverts each transaction's balance effect on accounts
   - Calls transactionDao.deleteAll()
   - Resets all account balances to their initialBalance
4. In SettingsScreen: add a "删除全部数据" card in a "数据管理" section
   - On click: show AlertDialog asking user to type "确认删除" to confirm
   - TextField for typing the confirmation
   - Button enabled only when text matches exactly "确认删除"
   - Calls viewModel.deleteAllTransactions()

5. SettingsViewModel: add deleteAllTransactions() method calling repository

## Feature 2: Theme Selection
1. Create ThemePreferencesManager at com.anaya.app.util.ThemePreferencesManager:
   - Uses SharedPreferences
   - Stores themeMode as String: "system" / "light" / "dark"
   - Methods: getThemeMode(): String, setThemeMode(mode: String)
   - @Singleton @Inject constructor

2. Register ThemePreferencesManager in AppModule.kt

3. Create com.anaya.app.presentation.theme.ThemeState.kt:
   - object ThemeState with mutableStateOf for current theme mode
   - Initialize from ThemePreferencesManager

4. In Theme.kt, make AnayaTheme accept themeMode parameter:
   - "system" -> isSystemInDarkTheme()
   - "light" -> lightColorScheme always
   - "dark" -> darkColorScheme always
   
5. Update MainActivity to pass themeMode from ThemePreferencesManager

6. In SettingsScreen: add "主题设置" section with 3 RadioButton options:
   - 跟随系统 (system)
   - 浅色 (light)
   - 深色 (dark)
   - On selection: save to ThemePreferencesManager and update ThemeState

## Feature 3: Data Backup & Restore
1. Create com.anaya.app.data.backup.BackupManager:
   - @Singleton @Inject constructor(context, transactionRepo, categoryRepo, accountRepo, budgetRepo)
   - backup(): creates JSON string with all data (transactions, categories, accounts, budgets), saves to Downloads/Anaya_备份_yyyyMMdd_HHmmss.json
   - restore(uri): reads JSON file, replaces all data in a Room transaction, resets balances

2. In SettingsScreen: add two buttons in "数据管理" section:
   - "导出备份" → saves backup file via BackupManager
   - "导入备份" → opens SAF file picker for .json, reads and restores

3. SettingsViewModel: add backupData() and restoreData(uri) methods

## Feature 4: Smart Icon in Classification
1. Read the ml/AutoClassifier.kt and related files
2. When auto-classifying a transaction and matching/creating a category:
   - If matching existing category: use the category's stored icon
   - If creating new category: assign a relevant emoji based on merchant/name keywords
3. Create a simple mapping: 餐饮→🍽️, 超市→🛒, 交通→🚌, 购物→🛍️, 娱乐→🎮, 医疗→💊, 教育→📚, 住房→🏠, 工资→💰, 红包→🧧, etc.
4. Ensure the icon flows through to TransactionDisplay and shows in HomeScreen

After ALL changes, run: cd /home/ayana/anaya && ./gradlew assembleDebug 2>&1 | tail -30
